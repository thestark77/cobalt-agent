name: cobalt-routing
version: 0.3.0
description: Per-task model routing + orchestrator tool guard for delegate_task
author: cobalt-agent
requires_hermes: ">=0.12.0,<1.0.0"
homepage: https://github.com/thestark77/cobalt-agent
provides_hooks:
  - pre_tool_call
